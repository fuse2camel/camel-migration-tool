## build and runcommand
```
  # Use defaults (8GB, 4 CPUs)
  ./setup_llm.sh

  # Increase memory and CPUs
  DOCKER_MEMORY=12g DOCKER_CPUS=6.0 ./setup_llm.sh

  # Max resources (adjust based on your Mac)
  DOCKER_MEMORY=16g DOCKER_CPUS=8.0 ./setup_llm.sh
```

